version: 0.2

phases:
   install:
      runtime-versions:
          java: corretto11

   build:
     commands:
        - java -version
        - cd webservice
        - mvn clean package

artifacts:
   files:
     - 'webservice-0.0.1-SNAPSHOT.jar'
   base-directory: 'target'
